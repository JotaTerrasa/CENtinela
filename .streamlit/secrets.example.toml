# Copia estos valores en Advanced settings > Secrets de Streamlit Community
# Cloud. No se necesita ninguna clave de modelo para la evidencia pública.
AI_PROVIDER = "codex"
EMBEDDING_PROVIDER = "local_hash"
# Mantener como texto para que Streamlit Community Cloud lo exponga también
# como variable de entorno consumible por pydantic-settings.
PUBLIC_DEMO_MODE = "true"
APP_ENV = "staging"
